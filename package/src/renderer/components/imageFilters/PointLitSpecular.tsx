import React from "react";

import type { PointLitSpecularImageFilterProps } from "../../../dom/types/ImageFilters";

export const PointLitSpecular = (props: PointLitSpecularImageFilterProps) => {
  return <skPointLitSpecularImageFilter {...props} />;
};
